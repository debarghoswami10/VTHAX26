{
  "name": "Booking",
  "type": "object",
  "properties": {
    "service_id": {
      "type": "string",
      "description": "Reference to Service"
    },
    "provider_id": {
      "type": "string",
      "description": "Reference to ServiceProvider"
    },
    "customer_name": {
      "type": "string",
      "description": "Customer full name"
    },
    "customer_phone": {
      "type": "string",
      "description": "Customer phone number"
    },
    "service_address": {
      "type": "string",
      "description": "Address where service will be provided"
    },
    "scheduled_date": {
      "type": "string",
      "format": "date",
      "description": "Scheduled service date"
    },
    "scheduled_time": {
      "type": "string",
      "description": "Scheduled service time"
    },
    "total_amount": {
      "type": "number",
      "description": "Total booking amount"
    },
    "status": {
      "type": "string",
      "enum": [
        "pending",
        "confirmed",
        "in_progress",
        "completed",
        "cancelled"
      ],
      "default": "pending",
      "description": "Booking status"
    },
    "special_instructions": {
      "type": "string",
      "description": "Special customer instructions"
    },
    "rating": {
      "type": "number",
      "description": "Customer rating after service completion"
    },
    "review": {
      "type": "string",
      "description": "Customer review"
    }
  },
  "required": [
    "service_id",
    "provider_id",
    "customer_name",
    "customer_phone",
    "service_address",
    "scheduled_date",
    "scheduled_time",
    "total_amount"
  ]
}